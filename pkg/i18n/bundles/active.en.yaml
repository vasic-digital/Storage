# Storage submodule — en bundle (CONST-046, round-118 + round-216)
# Keys prefixed with storage_ per cascade convention.
# Loaded by consuming projects via their own translator implementation;
# Storage's NoopTranslator returns the key verbatim and never reads this
# file, keeping the submodule project-not-aware per CONST-051(B).
storage_provider_aws_access_key_required: "access_key_id is required"
storage_provider_aws_secret_required: "secret_access_key is required"
storage_provider_aws_region_required: "region is required"
storage_provider_aws_credentials_not_configured: "AWS credentials not configured"
storage_resolver_no_backend_matched: "no backend matched path %q and no fallback configured"
# round-216 (CONST-046 Phase 4 round 95) — 10 user-facing strings migrated:
storage_provider_gcp_project_id_required: "project_id is required"
storage_provider_gcp_project_id_not_configured: "GCP project ID not configured"
storage_provider_azure_subscription_id_required: "subscription_id is required"
storage_provider_azure_tenant_id_required: "tenant_id is required"
storage_provider_azure_credentials_not_configured: "Azure credentials not configured"
storage_resolver_fallback_backend_not_found: "fallback backend %q not found"
storage_resolver_read_failure: "resolve for read: %w"
storage_resolver_write_failure: "resolve for write: %w"
storage_resolver_exists_failure: "resolve for exists: %w"
storage_resolver_delete_failure: "resolve for delete: %w"
